# Runtime dependencies.
# Phase 3 adds the agent orchestration (LangGraph). The Gemini/TTS SDKs arrive in
# Phase 4 — until then providers are mocks, so no AI keys are required to run.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
langgraph>=0.2
langchain-core>=0.3
google-genai>=1.0
